#!/usr/bin/env ruby

require 'rubygems'
require 'bundler'

Bundler.require

$: << 'lib'

require './config/config'
require 'cloud_balancer'

EM.run do
  runner = CloudBalancer::Runner.new
  block = lambda { close }
  runner.run!(:status) do |consumer|
    consumer.close
  end
end
