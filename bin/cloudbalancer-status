#!/usr/bin/env ruby

require 'rubygems'
require 'bundler'

Bundler.require

$: << 'lib'

require './config/config'
require 'cloud_balancer'

EM.run do
  runner = CloudBalancer::Runner.new
  runner.run!(:status)
end
